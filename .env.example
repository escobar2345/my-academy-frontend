# ============================================================
# BOI RSU FRONTEND ENVIRONMENT VARIABLES
# ============================================================
# Copy this file to .env and fill in your actual values.
# The ANON key is safe to expose in the browser (protected by RLS).


# Optional: direct API base when not using the Vite proxy in production
# VITE_BOI_API=http://localhost:5055

# Paystack public key (registration checkout)
VITE_PAYSTACK_PUBLIC_KEY=
